[project]
name = "habit"
version = "0.1.0"
description = "Template-based reminder scheduling core for an AI execution supervisor."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.27",
  "pydantic>=2.0",
  "jinja2>=3.0",
]

[tool.unittest]
start-directory = "tests"
